/** @type {import("next").NextConfig} */
const nextConfig = {
  // LLMings does not currently rely on the React Compiler; keep the setup simple.
  // If you later want to enable it, install `babel-plugin-react-compiler` and
  // reintroduce the `experimental.reactCompiler` flag here.
};

export default nextConfig;


